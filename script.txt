-- CRIANDO O BANCO DE DADOS
CREATE DATABASE BDASPB;

-- USANDO O BANCO DE DADOS
USE BDASPB;

-- CRIANDO AS TABELAS DO BANCO DE DADOS
CREATE TABLE Usuarios(
Id INT PRIMARY KEY auto_increment,
nome VARCHAR(40) NOT NULL,
Email VARCHAR(100) UNIQUE,
Senha VARCHAR(250) NOT NULL,
Nivel VARCHAR(40) DEFAULT 'Usuario',
DataCadastro DATETIME DEFAULT CURRENT_TIMESTAMP 
);

-- INSERINDO DADOS NO BANCO
INSERT INTO Usuarios(Nome,Email,Senha,Nivel)VALUES('Administrador', 'admin@email.com', '123456', 'Admin');
INSERT INTO Usuarios(Nome,Email,Senha)VALUES('Funcionário', 'usuario@email.com', '123456');

-- CONSULTANDO AS TABELAS DO BANCO
SELECT * FROM Usuarios;